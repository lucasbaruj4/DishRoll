# Ingredient Assets

This folder is reserved for ingredient icon image assets used by onboarding and inventory UIs.

Current implementation uses emoji-based icon tokens in `data/ingredientIcons.ts` keyed by `ingredient_catalog.icon_key`.
When we introduce PNG/SVG assets, place them here and map them by `icon_key`.
